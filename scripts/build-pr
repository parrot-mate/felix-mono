#!/bin/bash
set -e

# Keep branch arg for compatibility with existing workflow calls.
BRANCH="${1:-local}"
echo "Running PR build checks for branch: $BRANCH"

pnpm --filter @felix/blueprint-api run build
pnpm --filter @felix/blueprint-web run build
