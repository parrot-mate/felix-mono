{
  "name": "@pmate/proxy",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "pnpm run build && pm2 start ecosystem.config.cjs --only pmate-proxy"
  },
  "dependencies": {
    "express": "^4.18.2",
    "undici": "^7.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2"
  }
}
